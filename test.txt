w = w - learning_rate * g


velocity = momentum * velocity - learning_rate * g
w = w + velocity

velocity = momentum * velocity + (1 - momentum) * g
w = w - learning_rate * velocity



velocity = momentum * velocity - learning_rate * g
w = w + momentum * velocity - learning_rate * g

velocity = momentum * velocity + (1 - momentum) * g
velocity = g + momentum * velocity
w = w - learning_rate * velocity

velocity = momentum * velocity + (1 - momentum) * g
velocity = g + momentum * velocity
w = w - learning_rate * velocity